<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ikymasie.ny_times_api_assessment">

    <application
        android:name=".GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".views.NewsArticleListActivity"
            android:label="@string/title_newsarticle_list"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".views.NewsArticleDetailActivity"
            android:label="@string/title_newsarticle_detail"
            android:parentActivityName=".views.NewsArticleListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ikymasie.ny_times_api_assessment.views.NewsArticleListActivity" />
        </activity>
        <activity android:name=".BaseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>